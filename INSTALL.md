# 📦 Tile Set Up 설치 가이드

## 🚀 빠른 시작

### Windows 사용자

**방법 1: PowerShell 스크립트 (권장)**
```powershell
# PowerShell을 관리자 권한으로 실행 후
.\install.ps1
```

**방법 2: 배치 파일**
```
install.bat 파일을 더블클릭
```

**방법 3: 수동 설치**
```powershell
# Node.js 확인
node --version

# 의존성 설치
npm install

# 앱 실행
npx expo start --clear
```

---

### Mac/Linux 사용자

**방법 1: 설치 스크립트 (권장)**
```bash
# 실행 권한 부여
chmod +x install.sh

# 설치 실행
./install.sh
```

**방법 2: 수동 설치**
```bash
# Node.js 확인 (18 이상 필요)
node --version

# 의존성 설치
npm install

# 앱 실행
npx expo start --clear
```

---

## ✅ 설치 전 확인사항

### 필수 요구사항

1. **Node.js 18 이상**
   - 확인: `node --version`
   - 설치: https://nodejs.org

2. **npm** (Node.js와 함께 설치됨)
   - 확인: `npm --version`

3. **Expo Go 앱** (실제 기기 테스트용)
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Play Store](https://play.google.com/store/apps/details?id=host.exp.exponent)

### 선택 요구사항

- **Git** (GitHub에서 클론할 경우)
- **iOS Simulator** (Mac만, Xcode 필요)
- **Android Emulator** (Android Studio 필요)

---

## 📋 설치 단계별 가이드

### 1단계: 프로젝트 다운로드

**GitHub에서 클론:**
```bash
git clone https://github.com/masterpiece33330-prog/Tile-SetUp.git
cd Tile-SetUp
```

**또는 ZIP 파일 다운로드 후 압축 해제**

---

### 2단계: 설치 스크립트 실행

**Windows:**
- `install.ps1` 또는 `install.bat` 실행

**Mac/Linux:**
- `./install.sh` 실행

설치 스크립트가 자동으로:
- ✅ Node.js 및 npm 버전 확인
- ✅ Expo CLI 설치 확인
- ✅ 프로젝트 의존성 설치

---

### 3단계: 앱 실행

```bash
# 캐시 초기화와 함께 개발 서버 시작
npx expo start --clear
```

또는:

```bash
npm start
```

---

### 4단계: 앱 테스트

**실제 기기에서:**
1. 터미널에 나타난 **QR 코드** 확인
2. **Expo Go** 앱 실행
3. QR 코드 스캔
4. 앱 로딩 대기

**시뮬레이터/에뮬레이터:**
```bash
# iOS (Mac만)
npm run ios

# Android
npm run android
```

---

## 🔧 문제 해결

### Node.js가 설치되지 않음

**증상:**
```
❌ Node.js가 설치되어 있지 않습니다.
```

**해결:**
1. https://nodejs.org 방문
2. LTS 버전 다운로드 및 설치
3. 터미널 재시작
4. `node --version` 확인

---

### npm install 실패

**증상:**
```
npm ERR! code ELIFECYCLE
npm ERR! errno 1
```

**해결:**
```bash
# 캐시 정리
npm cache clean --force

# node_modules 삭제 후 재설치
rm -rf node_modules package-lock.json  # Mac/Linux
rmdir /s node_modules package-lock.json  # Windows

npm install
```

---

### Expo CLI 오류

**증상:**
```
Command 'expo' not found
```

**해결:**
```bash
# 전역 설치
npm install -g expo-cli

# 또는 npx 사용 (권장)
npx expo start
```

---

### 포트 충돌

**증상:**
```
Error: Port 8081 is already in use
```

**해결:**
```bash
# 다른 포트 사용
npx expo start --port 8082
```

---

### Metro 번들러 오류

**증상:**
```
Error: Unable to resolve module
```

**해결:**
```bash
# 캐시 초기화
npx expo start --clear

# 또는
watchman watch-del-all  # Mac/Linux
npm start -- --reset-cache
```

---

## 📱 예상 결과

설치가 성공하면 다음과 같은 화면이 나타납니다:

```
┌────────────────────────────┐
│                            │
│      🏗️ Tile Set Up        │
│  전문가를 위한 정밀 타일 시공 │
│                            │
│ ┌────────────────────────┐ │
│ │  + 새 프로젝트          │ │
│ └────────────────────────┘ │
│                            │
│ ┌────────────────────────┐ │
│ │  📁 최근 프로젝트       │ │
│ └────────────────────────┘ │
│                            │
│      ⚙️ 설정   v1.0.0      │
└────────────────────────────┘
```

---

## 🔄 GitHub에 업로드하기

설치가 완료되면 GitHub에 업로드할 수 있습니다:

```bash
# Git 초기화
git init

# 파일 추가
git add .

# 커밋
git commit -m "Initial commit: Tile Setup app"

# 원격 저장소 연결
git remote add origin https://github.com/masterpiece33330-prog/Tile-SetUp.git

# 브랜치 설정
git branch -M main

# 업로드
git push -u origin main
```

---

## 📞 도움이 필요하신가요?

문제가 계속되면 다음을 확인해주세요:

1. **에러 메시지 전체 내용** (스크린샷 또는 텍스트)
2. **운영체제 및 버전** (Windows 10/11, macOS, Linux)
3. **Node.js 버전** (`node --version`)
4. **npm 버전** (`npm --version`)

---

## ✅ 설치 체크리스트

- [ ] Node.js 18+ 설치됨
- [ ] npm 설치됨
- [ ] 프로젝트 폴더로 이동
- [ ] 설치 스크립트 실행 또는 `npm install` 완료
- [ ] `npx expo start --clear` 실행 성공
- [ ] Expo Go 앱 설치됨 (실제 기기 테스트용)
- [ ] QR 코드 스캔 성공
- [ ] 앱이 정상적으로 로딩됨

---

**설치 완료! 이제 개발을 시작하세요! 🎉**
